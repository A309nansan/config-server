services:
  vault:
    image: vault:1.13.3
    container_name: vault
    restart: unless-stopped
    volumes:
      - /var/vault:/vault
    networks:
      - nansan-network

networks:
  nansan-network:
    external: true